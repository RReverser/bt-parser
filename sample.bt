LittleEndian();
uint x;
local uint y = 9;

struct Point {
	float x;
	float y;
};

struct {
	float x;
	float y;
} point[10];

union {
	struct {
		uint8 hi;
		uint8 lo;
	} bytes;
	uint16 word;
} number;

Point p[100];

local uint8 b = ReadByte();

local string s1 = "some real \u0012 st\nring";
local string c = s1[0];

byte infinite[];